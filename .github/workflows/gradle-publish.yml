name: Gradle Package

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6

      - name: Validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6

      - name: Set up JDK 21 and 25
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: |
            21
            25
          cache: "gradle"

      - name: Resolve version with commit id
        id: version
        run: |
          BASE_VERSION="$(./gradlew -q printVersion --no-daemon)"
          COMMIT_SHORT="${GITHUB_SHA::8}"
          echo "full=${BASE_VERSION}-${COMMIT_SHORT}" >> "$GITHUB_OUTPUT"
          echo "Resolved version=${BASE_VERSION}-${COMMIT_SHORT}"

      - name: Build with Gradle
        run: chmod +x gradlew && ./gradlew shadowJar -x test --no-daemon -Pversion="${{ steps.version.outputs.full }}"

      - name: Upload PacketUxUi jar
        uses: actions/upload-artifact@v7
        with:
          name: packetuxui-build
          path: build/libs/PacketUxUi-*.jar
          if-no-files-found: error

      - name: Publish snapshot to Reposilite
        if: github.event_name == 'push'
        run: |
          if [ -z "${REPOSILITE_USER}" ] || [ -z "${REPOSILITE_TOKEN}" ]; then
            echo "Missing REPOSILITE_USER / REPOSILITE_TOKEN secrets (token name + secret)."
            exit 1
          fi
          ./gradlew publishSnapshot -x test --no-daemon -Pversion="${{ steps.version.outputs.full }}"
        env:
          REPOSILITE_USER: ${{ secrets.REPOSILITE_USER }}
          REPOSILITE_TOKEN: ${{ secrets.REPOSILITE_TOKEN }}
