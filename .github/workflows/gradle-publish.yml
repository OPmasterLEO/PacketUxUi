name: Gradle Package

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6

      - name: Validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6

      - name: Set up JDK 21 and 25
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: |
            21
            25
          cache: "gradle"

      - name: Build with Gradle
        run: chmod +x gradlew && ./gradlew shadowJar -x test --no-daemon

      - name: Upload PacketUxUi jar
        uses: actions/upload-artifact@v7
        with:
          name: packetuxui-build
          path: build/libs/PacketUxUi-*.jar
          if-no-files-found: error

      - name: Publish snapshot to Reposilite
        if: github.event_name == 'push'
        run: ./gradlew publish -x test -Preposilite.repo=snapshots --no-daemon
        env:
          REPOSILITE_USER: ${{ secrets.REPOSILITE_USER }}
          REPOSILITE_TOKEN: ${{ secrets.REPOSILITE_TOKEN }}
